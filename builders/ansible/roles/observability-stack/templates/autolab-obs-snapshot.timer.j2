# Managed by Ansible — roles/observability-stack.
[Unit]
Description=Prometheus snapshot to the NAS

[Timer]
OnCalendar={{ autolab_obs_backup_on_calendar }}
# Every lab host running the same timer would hit the NAS simultaneously.
RandomizedDelaySec=30m
Persistent=true

[Install]
WantedBy=timers.target
