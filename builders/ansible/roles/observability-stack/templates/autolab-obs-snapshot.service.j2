# Managed by Ansible — roles/observability-stack.
[Unit]
Description=Copy a Prometheus snapshot to the NAS
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/bin/autolab-obs-snapshot
