# Managed by Ansible — playbooks/backup.yml. Mode 0600; contains a token.
PBS_URL=https://{{ pbs.ansible_host }}:8007
PBS_TOKEN_ID={{ pbs.autolab_pbs_digest_token_id }}
PBS_TOKEN_SECRET={{ pbs.autolab_pbs_digest_token_secret }}
PBS_FINGERPRINT={{ pbs.autolab_pbs_fingerprint }}
PBS_DATASTORES={{ pbs.autolab_pbs_digest_datastores | join(',') }}
