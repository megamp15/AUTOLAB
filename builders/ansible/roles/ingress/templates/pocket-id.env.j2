# Managed by Ansible — roles/ingress.
APP_URL=https://{{ autolab_ingress_hostnames.auth }}.{{ autolab_ingress_zone }}
# Behind Traefik behind Cloudflare: the client address is in the headers.
TRUST_PROXY=true
ENCRYPTION_KEY={{ autolab_ingress_pocket_id_encryption_key }}
PUID=1000
PGID=1000
