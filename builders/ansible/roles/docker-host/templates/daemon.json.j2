{
  "_comment": "Managed by Ansible — roles/docker-host. Local edits are overwritten.",
  "log-driver": "{{ autolab_docker_log_driver }}",
  "log-opts": {
    "max-size": "{{ autolab_docker_log_max_size }}",
    "max-file": "{{ autolab_docker_log_max_file }}"
  }
}
