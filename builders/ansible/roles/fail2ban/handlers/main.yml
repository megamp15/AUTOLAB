---
- name: Restart fail2ban
  ansible.builtin.systemd_service:
    name: fail2ban
    state: restarted
  # Same as the start task: no unit exists yet in check mode on a fresh host.
  when: not (ansible_check_mode and autolab_fail2ban_install is changed)
