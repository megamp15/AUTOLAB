# Design sources

Exports from Claude Design, kept as the reference for pages the repo serves.

| File | Serves | Notes |
|---|---|---|
| `singularity.dc.html` | the homepage on the stack host (`roles/observability-stack/templates/homepage/index.html`) | Claude Design component; the port keeps its canvas, scramble and drag code and replaces the framework with ~40 lines of plain JS. Rows and feed come from `status.json`. |
